argocd:
  configs:
    cm:
      # ESO controller 가 admission/defaulting 으로 채워 ArgoCD diff 에서 OutOfSync 로 보이는
      # remoteRef/extract/find 의 default 필드들을 ignore. ClusterExternalSecret 은 spec 안에
      # externalSecretSpec 이 한 단계 nested 되어 있어 path 가 다르다.
      resource.customizations.ignoreDifferences.external-secrets.io_ExternalSecret: |
        jqPathExpressions:
        - .spec.data[].remoteRef.conversionStrategy
        - .spec.data[].remoteRef.decodingStrategy
        - .spec.data[].remoteRef.metadataPolicy
        - .spec.data[].remoteRef.nullBytePolicy
        - .spec.dataFrom[].extract.conversionStrategy
        - .spec.dataFrom[].extract.decodingStrategy
        - .spec.dataFrom[].extract.metadataPolicy
        - .spec.dataFrom[].extract.nullBytePolicy
        - .spec.dataFrom[].find.conversionStrategy
        - .spec.dataFrom[].find.decodingStrategy
        - .spec.dataFrom[].find.metadataPolicy
        - .spec.dataFrom[].find.nullBytePolicy
      resource.customizations.ignoreDifferences.external-secrets.io_ClusterExternalSecret: |
        jqPathExpressions:
        - .spec.externalSecretSpec.data[].remoteRef.conversionStrategy
        - .spec.externalSecretSpec.data[].remoteRef.decodingStrategy
        - .spec.externalSecretSpec.data[].remoteRef.metadataPolicy
        - .spec.externalSecretSpec.data[].remoteRef.nullBytePolicy
        - .spec.externalSecretSpec.dataFrom[].extract.conversionStrategy
        - .spec.externalSecretSpec.dataFrom[].extract.decodingStrategy
        - .spec.externalSecretSpec.dataFrom[].extract.metadataPolicy
        - .spec.externalSecretSpec.dataFrom[].extract.nullBytePolicy
        - .spec.externalSecretSpec.dataFrom[].find.conversionStrategy
        - .spec.externalSecretSpec.dataFrom[].find.decodingStrategy
        - .spec.externalSecretSpec.dataFrom[].find.metadataPolicy
        - .spec.externalSecretSpec.dataFrom[].find.nullBytePolicy

  global:
    domain: argocd.kkamji.net
  
  server:
    replicas: 1
    certificate:
      enabled: true
      issuer:
        group: cert-manager.io
        kind: ClusterIssuer
        name: letsencrypt-prod
    metrics:
      enabled: true
    # 외부 노출은 Envoy Gateway TLSRoute(charts/envoy-gateway)가 담당.
    # TLS 종료는 기존 ssl-passthrough 와 동일하게 argocd-server pod 가 직접 수행
    # (Certificate/argocd-server 는 server.certificate 가 계속 관리).
    extensions:
      enabled: true
      extensionList:
        - name: rollout-extension
          env:
            - name: EXTENSION_URL
              value: https://github.com/argoproj-labs/rollout-extension/releases/download/v0.3.7/extension.tar
  
  redis-ha:
    enabled: false
  
  controller:
    replicas: 1
  
  repoServer:
    replicas: 1
  
  applicationSet:
    replicas: 1
  
  notifications:
    enabled: true
    secret:
      create: false
      # External-Secrets 에서 불러오도록
      name: argocd-notifications-secret
      # secret name: argocd-notifications-secret
      # key: argocd-notifications-webhook
      # value: {discord webhook url}

    notifiers:
      # Discord 는 native notification service 가 없어 generic webhook service 로 전송한다.
      # url 의 $argocd-notifications-webhook 는 argocd-notifications-secret 의 동명 key 로
      # 치환된다 (notifications-engine 이 모든 scalar 필드에서 $var 를 secret 값으로 치환).
      service.webhook.discord: |
        url: $argocd-notifications-webhook
        headers:
          - name: Content-Type
            value: application/json

    subscriptions:
      # ArgoCD 전용 단일 Discord webhook(대상 채널에 바인딩). webhook 은 url 이 채널을
      # 고정하므로 recipient 는 service 이름(discord)만 지정한다.
      # 전역 구독은 실패 계열만 유지한다. 배포 성공(on-deployed) 알림은
      # Application 별 notifications.argoproj.io/subscribe.on-deployed.discord
      # annotation 으로 opt-in 한다 (monorepo 특성상 전역 구독 시 커밋마다 다발성 noise).
      - recipients:
          - discord
        triggers:
          - on-sync-failed
          - on-health-degraded
          - on-sync-status-unknown
    templates:
      # Discord webhook 은 template 의 webhook.discord.body(JSON) 만 사용한다.
      # 자유 텍스트(condition/error message)는 toJson 으로 escape, default 로 빈 값 방지
      # (Discord embed field value 는 non-empty 필수), trunc 1000 으로 1024 자 상한 회피.
      # color 는 hex 를 십진수로 변환한 embed color. email subject 는 참고용으로 유지.
      template.app-deployed: |
        email:
          subject: New version of an application {{.app.metadata.name}} is up and running.
        webhook:
          discord:
            method: POST
            body: |
              {
                "content": {{ printf "New version of `%s` is up and running." .app.metadata.name | toJson }},
                "embeds": [
                  {
                    "title": {{ .app.metadata.name | toJson }},
                    "url": "{{ .context.argocdUrl }}/applications/{{ .app.metadata.name }}",
                    "color": 1621586,
                    "fields": [
                      { "name": "Sync Status", "value": {{ .app.status.sync.status | default "-" | toJson }}, "inline": true },
                      { "name": "Repository", "value": {{ .app.spec.source.repoURL | default "-" | toJson }}, "inline": true },
                      { "name": "Revision", "value": {{ .app.status.sync.revision | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- range $c := .app.status.conditions }},
                      { "name": {{ $c.type | toJson }}, "value": {{ $c.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- end }}
                    ]
                  }
                ]
              }
      template.app-health-degraded: |
        email:
          subject: Application {{.app.metadata.name}} has degraded.
        webhook:
          discord:
            method: POST
            body: |
              {
                "content": {{ printf "@here Application `%s` has degraded." .app.metadata.name | toJson }},
                "allowed_mentions": { "parse": ["everyone"] },
                "embeds": [
                  {
                    "title": {{ .app.metadata.name | toJson }},
                    "url": "{{ .context.argocdUrl }}/applications/{{ .app.metadata.name }}",
                    "color": 16039984,
                    "fields": [
                      { "name": "Sync Status", "value": {{ .app.status.sync.status | default "-" | toJson }}, "inline": true },
                      { "name": "Repository", "value": {{ .app.spec.source.repoURL | default "-" | toJson }}, "inline": true }
                      {{- range $c := .app.status.conditions }},
                      { "name": {{ $c.type | toJson }}, "value": {{ $c.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- end }}
                    ]
                  }
                ]
              }
      template.app-sync-failed: |
        email:
          subject: Failed to sync application {{.app.metadata.name}}.
        webhook:
          discord:
            method: POST
            body: |
              {
                "content": {{ printf "@here Sync failed for `%s`." .app.metadata.name | toJson }},
                "allowed_mentions": { "parse": ["everyone"] },
                "embeds": [
                  {
                    "title": {{ .app.metadata.name | toJson }},
                    "url": "{{ .context.argocdUrl }}/applications/{{ .app.metadata.name }}?operation=true",
                    "color": 15297910,
                    "fields": [
                      { "name": "Sync Status", "value": {{ .app.status.sync.status | default "-" | toJson }}, "inline": true },
                      { "name": "Repository", "value": {{ .app.spec.source.repoURL | default "-" | toJson }}, "inline": true },
                      { "name": "Error", "value": {{ .app.status.operationState.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- range $c := .app.status.conditions }},
                      { "name": {{ $c.type | toJson }}, "value": {{ $c.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- end }}
                    ]
                  }
                ]
              }
      template.app-sync-running: |
        email:
          subject: Start syncing application {{.app.metadata.name}}.
        webhook:
          discord:
            method: POST
            body: |
              {
                "content": {{ printf "Syncing `%s`..." .app.metadata.name | toJson }},
                "embeds": [
                  {
                    "title": {{ .app.metadata.name | toJson }},
                    "url": "{{ .context.argocdUrl }}/applications/{{ .app.metadata.name }}?operation=true",
                    "color": 896490,
                    "fields": [
                      { "name": "Sync Status", "value": {{ .app.status.sync.status | default "-" | toJson }}, "inline": true },
                      { "name": "Repository", "value": {{ .app.spec.source.repoURL | default "-" | toJson }}, "inline": true }
                      {{- range $c := .app.status.conditions }},
                      { "name": {{ $c.type | toJson }}, "value": {{ $c.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- end }}
                    ]
                  }
                ]
              }
      template.app-sync-status-unknown: |
        email:
          subject: Application {{.app.metadata.name}} sync status is 'Unknown'
        webhook:
          discord:
            method: POST
            body: |
              {
                "content": {{ printf "Application `%s` sync status is Unknown." .app.metadata.name | toJson }},
                "embeds": [
                  {
                    "title": {{ .app.metadata.name | toJson }},
                    "url": "{{ .context.argocdUrl }}/applications/{{ .app.metadata.name }}",
                    "color": 15297910,
                    "fields": [
                      { "name": "Sync Status", "value": {{ .app.status.sync.status | default "-" | toJson }}, "inline": true },
                      { "name": "Repository", "value": {{ .app.spec.source.repoURL | default "-" | toJson }}, "inline": true }
                      {{- range $c := .app.status.conditions }},
                      { "name": {{ $c.type | toJson }}, "value": {{ $c.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- end }}
                    ]
                  }
                ]
              }
      template.app-sync-succeeded: |
        email:
          subject: Application {{.app.metadata.name}} has been successfully synced.
        webhook:
          discord:
            method: POST
            body: |
              {
                "content": {{ printf "Application `%s` has been successfully synced." .app.metadata.name | toJson }},
                "embeds": [
                  {
                    "title": {{ .app.metadata.name | toJson }},
                    "url": "{{ .context.argocdUrl }}/applications/{{ .app.metadata.name }}?operation=true",
                    "color": 1621586,
                    "fields": [
                      { "name": "Sync Status", "value": {{ .app.status.sync.status | default "-" | toJson }}, "inline": true },
                      { "name": "Repository", "value": {{ .app.spec.source.repoURL | default "-" | toJson }}, "inline": true }
                      {{- range $c := .app.status.conditions }},
                      { "name": {{ $c.type | toJson }}, "value": {{ $c.message | default "-" | trunc 1000 | toJson }}, "inline": false }
                      {{- end }}
                    ]
                  }
                ]
              }
    triggers:
      trigger.on-deployed: |
        - description: Application is synced and healthy. Triggered once per commit.
          # mono-repo 환경에서는 sync.revision 대신 syncResult.revision 사용
          oncePer: app.status.operationState.syncResult.revision
          send:
          - app-deployed
          when: app.status.operationState != nil and app.status.operationState.phase in ['Succeeded'] and app.status.health.status == 'Healthy'
      trigger.on-health-degraded: |
        - description: Application has degraded
          send:
          - app-health-degraded
          when: app.status.health.status == 'Degraded'
      trigger.on-sync-failed: |
        - description: Application syncing has failed
          send:
          - app-sync-failed
          when: app.status.operationState != nil and app.status.operationState.phase in ['Error', 'Failed']
      trigger.on-sync-running: |
        - description: Application is being synced
          send:
          - app-sync-running
          when: app.status.operationState != nil and app.status.operationState.phase in ['Running']
      trigger.on-sync-status-unknown: |
        - description: Application status is 'Unknown'
          send:
          - app-sync-status-unknown
          when: app.status.sync.status == 'Unknown'
      # For more information: https://argo-cd.readthedocs.io/en/stable/operator-manual/notifications/triggers/#default-triggers
      defaultTriggers: |
        - on-sync-status-unknown

argo-projects:
  namespace: argocd
  
  projects:
    argocd:
      description: "ArgoCD self-management (argo-cd, argocd-projects, root-applications)"
      sourceRepos:
        - "*"
      destinations:
        - server: https://kubernetes.default.svc
          namespace: "*"
      clusterResourceWhitelist:
        - group: "*"
          kind: "*"
      namespaceResourceWhitelist:
        - group: "*"
          kind: "*"
  
    management:
      description: "Cluster management addons (CNI, ingress, observability, security)"
      sourceRepos:
        - "*"
      destinations:
        - server: https://kubernetes.default.svc
          namespace: "*"
      clusterResourceWhitelist:
        - group: "*"
          kind: "*"
      namespaceResourceWhitelist:
        - group: "*"
          kind: "*"
  
    automation:
      description: "Automation tooling (openclaw, etc.)"
      sourceRepos:
        - "*"
      destinations:
        - server: https://kubernetes.default.svc
          namespace: "*"
      clusterResourceWhitelist:
        - group: "*"
          kind: "*"
      namespaceResourceWhitelist:
        - group: "*"
          kind: "*"
  
    personal:
      description: "Personal projects (play-hub)"
      sourceRepos:
        - "*"
      destinations:
        - server: https://kubernetes.default.svc
          namespace: "*"
      clusterResourceWhitelist:
        - group: "*"
          kind: "*"
      namespaceResourceWhitelist:
        - group: "*"
          kind: "*"

rootApplications:
  enabled: true
  namespace: argocd
  project: argocd
  repoURL: https://github.com/KKamJi98/helm-charts.git
  targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true

  roots:
    management-root:
      valueFiles:
        - management_local_values.yaml
    automation-root:
      valueFiles:
        - automation_local_values.yaml
    personal-root:
      valueFiles:
        - personal_local_values.yaml

# Admin 비밀번호를 ESO 로 고정 (SSM 값은 bcrypt 해시).
# 재구성/부트스트랩 직후에도 initial-admin-secret 대신 지정 비밀번호로 수렴.
adminPassword:
  enabled: true
  ssmKey: /kkamji/argocd/admin-password-bcrypt

# Repo credentials via ESO. SSM parameters must be pre-populated.
# Single GitHub App registered with two installations (KKamJi98 + kube-rca).
# App ID + private key are shared; only installationID differs per prefix.
repoCredentials:
  github-app-creds-personal:
    enabled: true
    url: "https://github.com/KKamJi98/"
    refreshInterval: 1h0m0s
    secretStoreRef:
      name: parameter-store
      kind: ClusterSecretStore
    ssm:
      appID: /kkamji/argocd/github-app/app-id
      installationID: /kkamji/argocd/github-app/installation-id-personal
      privateKey: /kkamji/argocd/github-app/private-key

  github-app-creds-kuberca:
    enabled: true
    url: "https://github.com/kube-rca/"
    refreshInterval: 1h0m0s
    secretStoreRef:
      name: parameter-store
      kind: ClusterSecretStore
    ssm:
      appID: /kkamji/argocd/github-app/app-id
      installationID: /kkamji/argocd/github-app/installation-id-kuberca
      privateKey: /kkamji/argocd/github-app/private-key

# GitHub webhook secret. ESO 가 argocd-secret 에 webhook.github.secret 키만
# merge 로 추가 → repo push 즉시 ArgoCD sync 트리거 가능.
githubWebhook:
  enabled: true
  refreshInterval: 1h0m0s
  secretStoreRef:
    name: parameter-store
    kind: ClusterSecretStore
  ssmKey: /kkamji/argocd/github-webhook/secret

# Discord webhook for argocd-notifications. ESO 가 argocd-notifications-secret 에
# argocd-notifications-webhook 키를 sync → service.webhook.discord.url 이 $변수로 참조.
discordWebhook:
  ssmKey: /kkamji/monitoring/discord/argocd-notifications-webhook

# Gateway API TLSRoute (앱 차트 소유 - templates/gateway-route.yaml).
# argocd-server pod 가 직접 TLS 종료하므로 플랫폼 Gateway 의 passthrough
# 리스너(tls-argocd)에 붙는다.
gatewayRoute:
  enabled: true
  host: argocd.kkamji.net
  backend: { name: argocd-server, port: 443 }
  parentRef:
    name: kkamji
    namespace: envoy-gateway-system
    sectionName: tls-argocd
